<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hevy_background">

    <!-- Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/hevy_blue"
        android:title="Adicionar Projeto"
        android:titleTextColor="#FFFFFF"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ScrollView -->
    <ScrollView
        android:id="@+id/scrollTarefas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/topAppBar"
        app:layout_constraintBottom_toTopOf="@+id/btnSalvarProjeto"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/containerCampos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Título do projeto -->
            <com.google.android.material.textfield.TextInputLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Título do projeto"
                android:layout_marginBottom="16dp">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/editTituloProjeto"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="#FFFFFF"
                    android:textColorHint="#5D5B5B"/>
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Container para tarefas -->
            <LinearLayout
                android:id="@+id/containerTarefas"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:divider="@color/hevy_background"
                android:showDividers="middle"
                android:paddingVertical="8dp"/>

            <!-- Botão adicionar tarefa -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAdicionarTarefa"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Adicionar Tarefa"
                android:textColor="#FFFFFF"
                android:layout_marginTop="16dp"
                android:layout_gravity="center_horizontal"
                app:cornerRadius="24dp"
                app:backgroundTint="@color/hevy_blue"/>

            <!-- Container para usuários -->
            <LinearLayout
                android:id="@+id/containerUsuarios"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="24dp"
                android:divider="@color/hevy_background"
                android:showDividers="middle"
                android:paddingVertical="8dp"/>

            <!-- Botão adicionar responsável -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAdicionarUsuario"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Adicionar Responsável"
                android:textColor="#FFFFFF"
                android:layout_marginTop="16dp"
                android:layout_gravity="center_horizontal"
                app:cornerRadius="24dp"
                app:backgroundTint="@color/hevy_blue"/>

        </LinearLayout>
    </ScrollView>

    <!-- Botão Salvar projeto -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSalvarProjeto"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Salvar Projeto"
        android:textColor="#FFFFFF"
        android:layout_margin="16dp"
        app:cornerRadius="24dp"
        app:backgroundTint="@color/hevy_blue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>
